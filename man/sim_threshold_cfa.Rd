% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sim_threshold_cfa.R
\name{sim_threshold_cfa}
\alias{sim_threshold_cfa}
\title{Estimate a CFA-based interpretation threshold for a continuous single-item measure}
\usage{
sim_threshold_cfa(
  mydata,
  var_formula,
  sim_levels = 10L,
  ordered = NULL,
  add_lmodel = NULL,
  tr_cut = 1,
  B = 0L,
  report_every = 50L,
  require_all_sim_levels = FALSE,
  factor_name = "F1",
  sim_suffix = "_ord",
  std.lv = TRUE,
  parameterization = "theta",
  verbose = FALSE,
  ...
)
}
\arguments{
\item{mydata}{Data frame.}

\item{var_formula}{Formula of the form \code{tr ~ sim + aux1 + aux2 + ...}.
The left-hand side is the transition or anchor variable. The first
right-hand side variable is the continuous SIM. Remaining right-hand side
variables are auxiliary CFA indicators.}

\item{sim_levels}{Number of equal-width levels for discretizing the SIM.
Must be between 2 and 12.}

\item{ordered}{Additional auxiliary variables to treat as ordered in lavaan.}

\item{add_lmodel}{Optional lavaan syntax appended to the generated model.}

\item{tr_cut}{Cutpoint for binarizing the transition variable when it has
more than two unique non-missing values. Values >= \code{tr_cut} are coded 1.
If the transition variable already has exactly two unique values, \code{tr_cut}
is ignored.}

\item{B}{Number of bootstrap samples. Bootstrap CI is computed only if
\code{B > 100}.}

\item{report_every}{During bootstrapping, print progress every
\code{report_every} attempted fits.}

\item{require_all_sim_levels}{Passed to \code{var_discretize()}.}

\item{factor_name}{Name of the latent factor.}

\item{sim_suffix}{Suffix appended to the SIM variable name after
discretization.}

\item{std.lv}{Passed to \code{lavaan::cfa()}.}

\item{parameterization}{Passed to \code{lavaan::cfa()}.}

\item{verbose}{If \code{TRUE}, print generated lavaan model.}

\item{...}{Additional arguments passed to \code{lavaan::cfa()}.}
}
\value{
A \code{sim_threshold_cfa} object. The printed output is compact.
Additional details can be retrieved with \code{sim_threshold_details()}.
}
\description{
\code{sim_threshold_cfa()} estimates an interpretation threshold for a continuous
single-item measure using a confirmatory factor analysis approach.
}
\details{
The technique is based on:

Terluin, B., Pua, Y.H., Fromy, P. et al. Estimating the minimal important
change of single-item measures using the adjusted predictive modeling method
or the longitudinal confirmatory factor analysis method. Qual Life Res 35,
39 (2026). DOI: 10.1007/s11136-025-04134-3

The \code{sim_threshold_cfa()} function:
\enumerate{
\item discretizes a continuous SIM into equal-width ordered categories;
\item fits a one-factor CFA model with an anchor transition item;
\item computes \code{theta_star}, the latent factor value where the anchor
threshold occurs;
\item maps \code{theta_star} back to the original SIM scale using CFA-implied
category probabilities and bin midpoints.
}
}

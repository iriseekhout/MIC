% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/mic_adjust.R
\name{mic_adjust}
\alias{mic_adjust}
\title{Predicted Minimal Important Change (MIC)}
\usage{
mic_adjust(data = NULL, x, y, tr, reliability)
}
\arguments{
\item{data}{data.frame that holds the scores at two time points and the
transition rate in separate columns.}

\item{x}{vector with score at time point 1, or column name in the data if
\code{!is.null(data)}}

\item{y}{vector with score at time point 1, or column name in the data if
\code{!is.null(data)}}

\item{tr}{vector with transition rates (perceived change), or column name in
the data if \code{!is.null(data)}}

\item{reliability}{the reliability for the transition score. Can be computed
with the \code{tr_reliability()} function.}
}
\value{
vector with the adjusted MIC value
}
\description{
Minimal Important Change (MIC) obtained as predicted value from a logistic
regression model.
}
\details{
\code{mic_adjust()} is a focused function for estimating the adjusted
predictive modeling-based MIC. For an all-in-one workflow that returns the
predictive MIC, adjusted predictive MIC, and improved adjusted predictive MIC
with optional bootstrap confidence intervals, see \code{\link[=mic_iapm]{mic_iapm()}}.
}
\examples{
data(example)
nitems <- 10
example$x <- rowSums(example[,1:nitems])                 # sumscore T1
example$y <- rowSums(example[,(nitems+1):(2*nitems)])    # sumscore T2
mic_adjust(x = example$x, y = example$y, tr = example$trat, reliability = 0.5)
mic_adjust(data = example, x = "x", y = "y", tr = "trat", reliability = 0.5)


# For predictive, adjusted, and improved adjusted MICs in one workflow,
# see mic_iapm().
}
\seealso{
\code{\link[=mic_iapm]{mic_iapm()}}, \code{\link[=tr_reliability]{tr_reliability()}}
}
